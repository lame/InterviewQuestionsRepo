USE truecar;

CREATE TABLE IF NOT EXISTS vehicle_by_serial_number(
    prefix text,
    remainder text,
    serial_number text,
    vehicle_trim int,
    year int,
    make text,
    model text,
    trim_name text,
    PRIMARY KEY ((prefix), remainder, vehicle_trim)
);
